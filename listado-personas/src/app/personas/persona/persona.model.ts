export class Persona{
  // nombre: string;
  // apellido: string;
  //
  // constructor(nombre:string, apellido:string){
  //   this.nombre = nombre;
  //   this.apellido = apellido;
  // }

  constructor(public nombre:string, public apellido:string){
    // shorthand mode, it's same to the before lines,
    // in this method creates the variables for the class at same that the constructor is called
    // only is syntax simplified
  }

}
